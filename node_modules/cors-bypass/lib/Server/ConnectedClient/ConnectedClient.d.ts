import { Handler } from '../../Handler';
import { ClientID, IServerTopics, IClientTopics } from '../../types';
import { Server } from '../Server';
export declare class ConnectedClient {
    private server;
    id: ClientID;
    private handlers;
    constructor(server: Server, id: ClientID);
    handleEvent<Topic extends keyof IServerTopics>(topic: Topic, payload: IServerTopics[Topic]): {
        handler: Handler;
        method: any;
        returnValue: any;
    };
    emit<Topic extends keyof IClientTopics>(topic: Topic, data: IClientTopics[Topic]): void;
    dispose(): void;
}
//# sourceMappingURL=ConnectedClient.d.ts.map