import { ConnectedClient } from '../ConnectedClient';
import { Handler } from '../../Handler';
import { Server } from '../Server';
export declare class ConnectedClientHandler implements Handler {
    private server;
    private client;
    private disposeTimer;
    constructor(server: Server, client: ConnectedClient);
    private addDisposeTimer;
    private clearDisposeTimer;
    ping(): void;
    dispose(): void;
}
//# sourceMappingURL=ConnectedClientHandler.d.ts.map