import { IClientTopics } from '../../types';
import { ConnectedClient } from '../ConnectedClient';
export declare type IServerOptions = {
    adapterUrl?: string;
};
export declare class Server {
    clients: Map<string, ConnectedClient>;
    private params;
    iframe: HTMLIFrameElement;
    id: string;
    constructor(options?: IServerOptions);
    private handleEvent;
    emit: <Topic extends "websocketInfo" | "websocketEvent" | "pong">(topic: Topic, data: IClientTopics[Topic], clientId?: string) => void;
}
//# sourceMappingURL=Server.d.ts.map