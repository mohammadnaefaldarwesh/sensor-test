import { ConnectedClient } from '../ConnectedClient';
import { Handler } from '../../Handler';
import { Server } from './Server';
export declare const PONG_INTERVAL = 1000;
export declare class ServerHandler implements Handler {
    private server;
    private client;
    private pongTimer;
    constructor(server: Server, client: ConnectedClient);
    private pong;
    pingAllServers(): void;
    disposeClient(): void;
    dispose(): void;
}
//# sourceMappingURL=ServerHandler.d.ts.map